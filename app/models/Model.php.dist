<?php
use Illuminate\Database\Eloquent\Model as Model;
class PLACE_YOUR_MODEL_NAME extends Model
{
    public function __construct(array $attributes = array())
    {
        Bootstrap::Eloquent();
        parent::__construct($attributes);
    }
}
